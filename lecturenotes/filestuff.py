# initialize a file object
my_file = open("myfile.txt", 'r') # opens a file in the current directory if the absolute path is not specified
                                  # mode 'r' means it is in read mode, it can only be read

# interact with the file
